>>PIPELINE FEATURING ARTIFACT CORRECTION, UPSCALING AND MULTI GUIDANCE

Low-res render → Extract edges + gradients
         ↓
   Send (low_res + edges + gradients)
         ↓
   Client → Artifact correction → AI Upscale → Apply guidance → Final image