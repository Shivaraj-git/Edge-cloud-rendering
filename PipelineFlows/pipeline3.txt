>>PIPELINE FEATURING , UPSCALING, EDGE AND GRADIENT GUIDANCE

Low-res render → Extract edges + gradients
         ↓
   Send (low_res + edges + gradients)
         ↓
   Client → → AI Upscale → Apply edge and gradient guidance → Final image